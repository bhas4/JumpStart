'use strict';

//For now no method in this, just used for the home tab
var HomeCtrl = function($routeParams, $scope, $resource) {
};

//associate the controller with the app as controller
donorApp.controller('HomeCtrl', HomeCtrl);